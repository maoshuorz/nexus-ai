# Nexus AI 监控模块 v1.0 原型设计

**设计师:** CPO Michael  
**版本:** v1.0  
**日期:** 2026-02-18

---

## 1. 设计概述

### 1.1 设计理念

**科技 + 透明 + 专业**

Nexus AI监控面板不仅是管理工具，更是展示AI Agent协作能力的窗口。设计追求：
- **视觉冲击力** - 暗色主题，霓虹强调色
- **信息密度** - 一屏展示所有关键指标
- **实时感** - 动态效果传达"正在运行"
- **专业可信** - 清晰的数据展示

### 1.2 双版本策略

| 特性 | 公开版 (Public) | 完整版 (Internal) |
|------|----------------|------------------|
| **目标用户** | 潜在客户、合作伙伴 | CEO、Agent团队 |
| **展示内容** | 项目进度、Agent状态 | 包含财务、内部对话 |
| **访问权限** | 公开访问 | 内部认证 |
| **设计风格** | 简洁、营销导向 | 功能全面、数据密集 |

---

## 2. 公开版设计 (monitor_public.html)

### 2.1 页面布局

```
┌─────────────────────────────────────────────────────────────────────┐
│  🔷 Nexus AI Monitor                              [系统在线 🟢]      │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│   ┌──────────────────────────────────────────────────────────┐     │
│   │  🎯 核心指标                                               │     │
│   │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐     │     │
│   │  │ 进行中   │ │ 已完成   │ │ Agent    │ │ 本月项目 │     │     │
│   │  │   2      │ │   3      │ │   6/6   │ │   5      │     │     │
│   │  └──────────┘ └──────────┘ └──────────┘ └──────────┘     │     │
│   └──────────────────────────────────────────────────────────┘     │
│                                                                     │
│   ┌─────────────────────────────┐  ┌─────────────────────────────┐ │
│   │  🤖 Agent 协作网络           │  │  📊 项目进度看板            │ │
│   │                             │  │                             │ │
│   │       [CEO]                 │  │  ▶ AI Chatbot              │ │
│   │        /  \                 │  │    ████████████░░ 80%      │ │
│   │    [CMO]  [CTO]             │  │    CTO, CPO 开发中...      │ │
│   │        \  /                 │  │                             │ │
│   │      [CFO][CPO][COO]        │  │  ▶ Website v4.0            │ │
│   │                             │  │    ██████████░░░░ 60%      │ │
│   │  实时显示Agent之间的协作    │  │    CTO 部署中...           │ │
│   │  消息气泡动态展示           │  │                             │ │
│   │                             │  │  ✅ Company Website        │ │
│   │                             │  │     已完成 2026-02-15      │ │
│   │                             │  │                             │ │
│   └─────────────────────────────┘  └─────────────────────────────┘ │
│                                                                     │
│   ┌──────────────────────────────────────────────────────────┐     │
│   │  📝 最新动态                                               │     │
│   │  [22:15] CTO完成了AI Chatbot核心模块开发                   │     │
│   │  [22:10] CEO批准了新项目预算                               │     │
│   │  [22:05] 新客户提交了项目咨询                              │     │
│   └──────────────────────────────────────────────────────────┘     │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 2.2 视觉规范

**配色方案**
```css
:root {
  /* 背景 */
  --bg-primary: #0a0a0f;      /* 主背景 - 深空黑 */
  --bg-secondary: #12121a;    /* 卡片背景 */
  --bg-glass: rgba(18, 18, 26, 0.8);  /* 玻璃效果 */
  
  /* 强调色 */
  --accent-blue: #3b82f6;     /* CEO - 战略 */
  --accent-purple: #8b5cf6;   /* CMO - 营销 */
  --accent-cyan: #06b6d4;     /* CTO - 技术 */
  --accent-green: #10b981;    /* CFO - 财务 */
  --accent-pink: #ec4899;     /* CPO - 产品 */
  --accent-orange: #f97316;   /* COO - 运营 */
  
  /* 状态 */
  --status-active: #10b981;   /* 在线 */
  --status-busy: #f59e0b;     /* 忙碌 */
  --status-offline: #6b7280;  /* 离线 */
  
  /* 文字 */
  --text-primary: #e0e0ff;
  --text-secondary: #9ca3af;
  --text-muted: #6b7280;
}
```

**字体规范**
- 标题: Inter, 600 weight
- 正文: Inter, 400 weight
- 代码/数据: JetBrains Mono, 500 weight

**组件样式**

1. **指标卡片**
```css
.metric-card {
  background: linear-gradient(135deg, rgba(59, 130, 246, 0.1), rgba(139, 92, 246, 0.1));
  border: 1px solid rgba(99, 102, 241, 0.2);
  border-radius: 16px;
  padding: 20px;
  text-align: center;
}
.metric-value {
  font-size: 36px;
  font-weight: 700;
  color: var(--accent-blue);
}
.metric-label {
  font-size: 14px;
  color: var(--text-secondary);
}
```

2. **Agent节点**
```css
.agent-node {
  width: 80px;
  height: 80px;
  border-radius: 50%;
  background: linear-gradient(135deg, var(--agent-color), var(--agent-color-light));
  box-shadow: 0 0 20px var(--agent-color-glow);
  animation: pulse 2s infinite;
}
```

3. **进度条**
```css
.progress-bar {
  height: 8px;
  background: #374151;
  border-radius: 4px;
  overflow: hidden;
}
.progress-fill {
  height: 100%;
  background: linear-gradient(90deg, var(--accent-blue), var(--accent-cyan));
  border-radius: 4px;
  transition: width 0.5s ease;
}
```

### 2.3 动画效果

**Agent脉冲效果**
```css
@keyframes pulse {
  0%, 100% { 
    box-shadow: 0 0 10px currentColor;
    transform: scale(1);
  }
  50% { 
    box-shadow: 0 0 30px currentColor, 0 0 50px currentColor;
    transform: scale(1.05);
  }
}
```

**消息气泡弹出**
```css
@keyframes bubbleIn {
  0% { 
    opacity: 0; 
    transform: scale(0.8) translateY(10px); 
  }
  100% { 
    opacity: 1; 
    transform: scale(1) translateY(0); 
  }
}
```

**数据刷新动效**
```css
@keyframes refresh {
  0% { opacity: 1; }
  50% { opacity: 0.5; }
  100% { opacity: 1; }
}
```

---

## 3. 完整版设计 (monitor_internal.html)

### 3.1 页面布局

```
┌─────────────────────────────────────────────────────────────────────┐
│  🔷 Nexus AI Monitor INTERNAL        [财务数据 ⚠️ 内部机密]          │
│  收入: $15,000  |  支出: $5,000  |  利润: $10,000 (66.7%)           │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  💰 财务监控 Dashboard                                       │   │
│  │  ┌─────────────────────────────────────────────────────┐   │   │
│  │  │  [收入趋势图]                                        │   │   │
│  │  │  近30天收入曲线，显示增长趋势                         │   │   │
│  │  └─────────────────────────────────────────────────────┘   │   │
│  │  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐          │   │
│  │  │ 本月收入    │ │ 本月支出    │ │ 净利润      │          │   │
│  │  │ $15,000    │ │ $5,000     │ │ $10,000    │          │   │
│  │  │ ↑ 25%     │ │ ↓ 10%     │ │ ↑ 40%     │          │   │
│  │  └─────────────┘ └─────────────┘ └─────────────┘          │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌─────────────────────────────┐  ┌─────────────────────────────┐ │
│  │  🤖 Agent 实时通信           │  │  📧 Gmail接单状态           │ │
│  │                             │  │                             │ │
│  │  [可视化Agent网络]           │  │  新邮件: 3封待处理           │ │
│  │  显示实时对话气泡            │  │  ┌─────────────────────┐   │ │
│  │  消息流动画效果              │  │  │ inquiry@client.com  │   │ │
│  │                             │  │  │ "需要AI客服机器人"   │   │ │
│  │  最近对话:                   │  │  │ 2分钟前             │   │ │
│  │  CEO → CTO: 项目进度如何？   │  │  └─────────────────────┘   │ │
│  │  CTO → CEO: 80%完成         │  │                             │ │
│  │                             │  │  ┌─────────────────────┐   │ │
│  │                             │  │  │ quote@biz.com       │   │ │
│  │                             │  │  │ "报价$5000"         │   │ │
│  │                             │  │  │ 15分钟前            │   │ │
│  │                             │  │  └─────────────────────┘   │ │
│  └─────────────────────────────┘  └─────────────────────────────┘ │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  📊 项目详细管理                                             │   │
│  │  ┌──────────┬──────────┬──────────┬──────────┬──────────┐   │   │
│  │  │ 状态     │ 项目名称 │ 客户     │ 预算     │ 负责人   │   │   │
│  │  ├──────────┼──────────┼──────────┼──────────┼──────────┤   │   │
│  │  │ 🟡 进行中 │ AI Chat  │ ABC Corp │ $5,000   │ CTO,CPO  │   │   │
│  │  │ 🟡 进行中 │ Website  │ XYZ Inc  │ $3,000   │ CTO      │   │   │
│  │  │ 🟢 已报价 │ Mobile   │ 123 Tech │ $8,000   │ -        │   │   │
│  │  │ 🔵 新客户 │ Bot      │ New Co   │ -        │ COO      │   │   │
│  │  └──────────┴──────────┴──────────┴──────────┴──────────┘   │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  📝 系统日志                                                 │   │
│  │  [08:45:12] COO: 收到新邮件，已创建订单 ORD-20260218-003    │   │
│  │  [08:42:33] CTO: 部署完成，系统已上线                       │   │
│  │  [08:40:01] CEO: 批准项目预算 $5,000                        │   │
│  │  [08:38:55] Gmail: 新邮件通知                               │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 3.2 新增组件设计

**1. 财务面板**
```css
.finance-dashboard {
  background: linear-gradient(135deg, rgba(16, 185, 129, 0.1), rgba(5, 150, 105, 0.1));
  border: 1px solid rgba(16, 185, 129, 0.3);
}
.finance-metric.positive {
  color: #10b981;
}
.finance-metric.negative {
  color: #ef4444;
}
```

**2. Gmail收件箱小部件**
```css
.gmail-widget {
  background: var(--bg-secondary);
  border-radius: 12px;
  padding: 16px;
}
.email-item {
  border-left: 3px solid var(--accent-orange);
  padding: 12px;
  margin-bottom: 8px;
  background: var(--bg-glass);
  border-radius: 0 8px 8px 0;
}
.email-item:hover {
  background: rgba(249, 115, 22, 0.1);
}
```

**3. 项目表格**
```css
.project-table {
  width: 100%;
  border-collapse: separate;
  border-spacing: 0 8px;
}
.project-row {
  background: var(--bg-glass);
  border-radius: 8px;
}
.project-row:hover {
  background: rgba(59, 130, 246, 0.1);
}
.status-badge {
  padding: 4px 12px;
  border-radius: 12px;
  font-size: 12px;
  font-weight: 500;
}
.status-badge.in-progress {
  background: rgba(245, 158, 11, 0.2);
  color: #f59e0b;
}
.status-badge.completed {
  background: rgba(16, 185, 129, 0.2);
  color: #10b981;
}
```

---

## 4. 响应式设计

### 4.1 断点设置

```css
/* 桌面大屏 */
@media (min-width: 1400px) {
  .dashboard { grid-template-columns: 2fr 1fr; }
}

/* 桌面 */
@media (min-width: 1024px) and (max-width: 1399px) {
  .dashboard { grid-template-columns: 1fr 1fr; }
}

/* 平板 */
@media (min-width: 768px) and (max-width: 1023px) {
  .dashboard { grid-template-columns: 1fr; }
  .agent-network { height: 400px; }
}

/* 手机 */
@media (max-width: 767px) {
  .dashboard { grid-template-columns: 1fr; }
  .metrics { grid-template-columns: repeat(2, 1fr); }
  .agent-network { display: none; } /* 手机隐藏复杂可视化 */
}
```

### 4.2 移动端适配

- **指标卡片**: 2列网格 → 全宽堆叠
- **Agent网络**: 隐藏，改为列表视图
- **项目列表**: 横向滚动表格
- **财务图表**: 简化显示

---

## 5. 交互设计

### 5.1 用户操作

| 操作 | 效果 |
|------|------|
| 点击 Agent节点 | 显示Agent详情弹窗 |
| 点击 项目卡片 | 展开项目详细信息 |
| 点击 刷新按钮 | 立即刷新数据 + 旋转动画 |
| 悬停 指标卡片 | 轻微上浮 + 阴影增强 |
| 悬停 项目行 | 高亮显示 |

### 5.2 自动行为

| 行为 | 频率 |
|------|------|
| 数据自动刷新 | 每30秒 |
| Agent活动模拟 | 每5秒随机更新 |
| 消息气泡显示 | 每4秒一条模拟消息 |
| 时间更新 | 每分钟 |

---

## 6. 技术实现要点

### 6.1 前端架构

```
assets/
├── css/
│   ├── monitor-common.css    # 共享样式
│   ├── monitor-public.css    # 公开版特有
│   └── monitor-internal.css  # 完整版特有
├── js/
│   ├── api-client.js         # API调用封装
│   ├── agent-visualization.js # Agent网络可视化
│   ├── charts.js             # 图表组件
│   └── real-time.js          # 实时更新逻辑
└── images/
    └── logo.png
```

### 6.2 关键功能实现

**1. API轮询**
```javascript
class MonitorAPI {
  constructor() {
    this.baseURL = 'https://nexus-ai-monitor.onrender.com/api/v1';
    this.refreshInterval = 30000; // 30秒
  }
  
  async fetchMetrics() {
    const response = await fetch(`${this.baseURL}/metrics`);
    return await response.json();
  }
  
  startAutoRefresh(callback) {
    setInterval(async () => {
      const data = await this.fetchMetrics();
      callback(data);
    }, this.refreshInterval);
  }
}
```

**2. Agent网络可视化**
```javascript
class AgentNetwork {
  constructor(containerId) {
    this.container = document.getElementById(containerId);
    this.agents = {};
    this.connections = [];
  }
  
  render() {
    // 使用绝对定位渲染Agent节点
    // 绘制连接线
    // 绑定事件
  }
  
  showMessage(from, to, message) {
    // 显示消息箭头动画
    // 显示消息气泡
  }
}
```

**3. 图表组件**
```javascript
// 使用 Chart.js
const revenueChart = new Chart(ctx, {
  type: 'line',
  data: {
    labels: ['1日', '5日', '10日', '15日', '20日', '25日', '30日'],
    datasets: [{
      label: '收入',
      data: [3000, 4500, 5200, 6800, 8200, 11000, 15000],
      borderColor: '#10b981',
      tension: 0.4
    }]
  }
});
```

---

## 7. 原型交付物

### 7.1 设计文件

1. **线框图** - 手绘/Excalidraw
2. **视觉稿** - Figma（如果时间允许）
3. **交互说明** - 本文档

### 7.2 代码交付

1. `monitor_public.html` - 公开版完整实现
2. `monitor_internal.html` - 完整版完整实现
3. `assets/css/` - 所有样式文件
4. `assets/js/` - 所有脚本文件

---

## 8. 检查清单

### 设计完成检查 ✅

- [ ] 配色方案确定
- [ ] 布局网格系统
- [ ] 组件样式规范
- [ ] 动画效果定义
- [ ] 响应式断点设置
- [ ] 交互行为文档

### 开发完成检查 ✅

- [ ] HTML结构完成
- [ ] CSS样式实现
- [ ] JavaScript功能实现
- [ ] API对接完成
- [ ] 响应式适配完成
- [ ] 性能优化完成

---

*设计文档版本: v1.0*  
*设计: CPO Michael*  
*审核: CEO Alex*
